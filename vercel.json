{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "public",
  "framework": null,
  "regions": ["icn1"],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api?__path=$1" }
  ]
}
